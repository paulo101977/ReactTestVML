

# ReactTestVML



## Usage

### Na pasta do projeto, executar a sequência de comandos:

1 - Instalar dependências nodeJS

npm install

2 - Instalar dependências do bower

bower install


3 - Compilar a aplicação:

npm run copy & webpack --env=dist

4 - Rodar um servidor local com a aplicação compilada:

gulp watch